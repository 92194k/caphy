# ---- Secrets: NEVER commit these ----
firebase_key.json
caphy_app/android/app/google-services.json

# ---- Python ----
venv/
__pycache__/
*.pyc

# ---- Model weights (large; download separately) ----
*.pt

# ---- YOLO training runs ----
runs/

# ---- Generated data (don't need to share) ----
caphy.db
captures/
captures_compressed/
cloud_sim/
models/

# ---- Flutter build junk ----
caphy_app/build/
caphy_app/.dart_tool/
# ---- runtime UI prefs (per-install) ----
ui_prefs.json

# ---- API keys (never commit) ----
caphy_keys.json

# ---- Device identity (per-machine, never commit) ----
device.json
